1.  **哺乳动物寿命。** 收集了62种哺乳动物的寿命（单位：年）和妊娠期（单位：天）的数据。下方展示了寿命与妊娠期长度的散点图。[^_05-ex-explore-numerical-1]
    [@Allison+Cicchetti:1975]

    :::: {layout="[-0.05, 0.5, -0.02, 0.43]"}

    :::{#questions}
    a.  寿命与妊娠期长度之间呈现出何种类型的关联？

    b.  如果你把图的坐标轴反过来，即绘制妊娠期长度与寿命的关系图，你预期会看到什么类型的关联？

    c.  寿命和妊娠期长度是独立的吗？请解释你的推理。
    :::

    :::{#plot}
    ```{r}
    #| fig-asp: 0.5
    #| fig-width: 4
    #| out-width: 100%
    #| fig-align: right
    ggplot(mammals, aes(x = gestation, y = life_span)) +
      geom_point(alpha = 0.8) +
      labs(x = "妊娠期（天）", y = "寿命（年）")
    ```
    :::
    ::::

\vfill

2.  **关联。** 请指出哪些图显示了 (a) 正关联，(b) 负关联，或 (c) 无关联。同时判断正关联和负关联是线性的还是非线性的。每个小题可能对应不止一张图。

    ```{r}
    #| fig-asp: 0.25
    #| out-width: 95%
    set.seed(2306)

    # create x
    x <- seq(0, 10, 0.1)

    # create y_poslin: positive linear with x
    y_poslin <- x * runif(1, min = 0, max = 10) +
      rnorm(length(x), mean = 0, sd = runif(1, min = 3, max = 10)) -
      runif(1, min = 0, max = 3)

    # create y_neglin: negative linear with x
    y_neglin <- x * -runif(1, min = 0, max = 5) +
      rnorm(length(x), mean = 0, sd = runif(1, min = 3, max = 10)) -
      runif(1, min = 0, max = 5)

    # create y_poscur: curved positive with x
    y_poscur <- x^2 + rnorm(length(x), mean = -3, sd = runif(1, min = 3, max = 4))

    # create y_none: no association with x
    y_none <- x + rnorm(length(x), mean = 0, sd = runif(1, min = 30, max = 40))

    # data frame
    tibble(
      x = x,
      poslin = y_poslin,
      neglin = y_neglin,
      poscur = y_poscur,
      none   = y_none
    ) |>
      pivot_longer(cols = -x, names_to = "type", values_to = "y") |>
      arrange(type) |>
      mutate(
        type_num = case_when(
          type == "poslin" ~ "(1)",
          type == "neglin" ~ "(4)",
          type == "poscur" ~ "(3)",
          type == "none"   ~ "(2)"
        ),
        type_num = as.factor(type_num)
        ) |>
      ggplot(aes(x = x, y = y)) +
      geom_point(alpha = 0.5) +
      facet_grid(~type_num) +
      theme(
        axis.text = element_blank()
      ) +
      labs(x = NULL, y = NULL)
    ```

\vfill

3.  **繁殖的细菌。** 假设一个培养皿中的空间和营养物质仅够支持一百万个细菌细胞。你在培养皿中放入少量细菌细胞，让它们自由繁殖，并记录一段时间内培养皿中的细菌细胞数量。请草图画出细菌细胞数量与时间的关系。

\vfill

4.  **办公效率。** 当员工对工作或工作保障感到无压力时，办公效率相对较低。然而，过高的压力水平也会导致员工效率下降。请草图画出压力与效率的关系。

\vfill

5.  **补考。** 一个班级有25名学生，其中24名在课上参加了考试，1名学生次日参加了补考。教授批改了第一批24份试卷，发现平均分数为74分，标准差为8.9分。次日参加补考的那名学生得了64分。

    a.  这位新学生的分数会使平均分数升高还是降低？

    b.  新的平均分数是多少？

    c.  这位新学生的分数会使分数的标准差升高还是降低？

\vfill

\clearpage

6.  **婴儿死亡率。** 婴儿死亡率定义为每1000名活产婴儿中的婴儿死亡数。该比率常被用作衡量一个国家健康水平的指标。下面的相对频数直方图显示了2014年有可用数据的224个国家的估计婴儿死亡率的分布情况。[^_05-ex-explore-numerical-2]

    :::: {layout="[-0.05, 0.5, -0.02, 0.43]"}

    :::{#questions}
    a.  根据直方图估计 Q1、中位数和 Q3。

    b.  你预期这个数据集的均值会比中位数大还是小？请解释你的推理。
    :::

    :::{#plot}
    ```{r}
    #| fig-asp: 0.5
    #| fig-width: 4
    #| out-width: 90%
    #| fig-align: right
    ggplot(cia_factbook, aes(x = infant_mortality_rate)) +
      geom_histogram(aes(y = stat(count) / sum(count)),
        binwidth = 10
      ) +
      labs(
        x = "婴儿死亡率（每1000名活产婴儿）",
        y = "比例"
      )
    ```
    :::

    ::::

7.  **某矿厂的休假天数。** 某特定矿场的工人平均有 35 天带薪休假，这低于全国平均水平。该矿厂的经理正面临当地工会要求增加带薪休假天数的压力。然而，他不想给工人更多假期，因为那样成本太高。相反，他决定通过解雇 10 名员工的方式来提高其员工报告的平均休假天数。为了达到这个目标，他应该解雇那些休假天数最多、最少，还是接近平均数的员工？

8.  **中位数和 IQR。** 对以下每个部分，根据中位数和 IQR 比较分布 A 和分布 B。你不需要计算这些统计量，只需比较并说明中位数和 IQR 的大小关系即可。请务必解释你的推理。*提示：* 绘制这些分布的点图可能有所帮助。

    a.  **A:** 3, 5, 6, 7, 9; **B:** 3, 5, 6, 7, 20

    b.  **A:** 3, 5, 6, 7, 9; **B:** 3, 5, 7, 8, 9

    c.  **A:** 1, 2, 3, 4, 5; **B:** 6, 7, 8, 9, 10

    d.  **A:** 0, 10, 50, 60, 100; **B:** 0, 100, 500, 600, 1000

9.  **均值和标准差。** 对以下每个部分，根据均值和标准差比较分布 A 和分布 B。你不需要计算这些统计量，只需比较并说明均值和标准差的大小关系即可。请务必解释你的推理。*提示：* 绘制这些分布的点图可能有所帮助。

    a.  **A:** 3, 5, 5, 5, 8, 11, 11, 11, 13; **B:** 3, 5, 5, 5, 8, 11, 11, 11, 20

    b.  **A:** -20, 0, 0, 0, 15, 25, 30, 30; **B:** -40, 0, 0, 0, 15, 25, 30, 30

    c.  **A:** 0, 2, 4, 6, 8, 10; **B:** 20, 22, 24, 26, 28, 30

    d.  **A:** 100, 200, 300, 400, 500; **B:** 0, 50, 300, 550, 600

10. **直方图和箱线图。** 用文字描述下图直方图的分布，并将它们与箱线图匹配。

    ```{r}
    #| out-width: 90%
    #| fig-asp: 0.37
    set.seed(7365)

    df <- tibble(
      sym = rnorm(1000, mean = 60, sd = 3),
      uni = runif(1000, min = 0, max = 100),
      rs  = rgamma(1000, shape = 3, rate = 2)
    ) |>
      pivot_longer(cols = everything(), names_to = "type", values_to = "x") |>
      arrange(type) |>
      mutate(type_label = case_when(
        type == "sym" ~ "(a)",
        type == "uni" ~ "(b)",
        type == "rs"  ~ "(c)",
      ))

    p_hist <- ggplot(df, aes(x = x)) +
      geom_histogram(bins = 10) +
      facet_wrap(~type_label, scales = "free") +
      theme(axis.text.y = element_blank()) +
      labs(x = "", y = "")

    p_box <- df |>
      mutate(type_label = case_when(
        type == "sym" ~ "(2)",
        type == "uni" ~ "(3)",
        type == "rs"  ~ "(1)",
      )) |>
      ggplot(aes(y = x)) +
      geom_boxplot(outlier.alpha = 0.8) +
      facet_wrap(~type_label, scales = "free") +
      theme(
        axis.text.x = element_blank(),
        panel.grid.major.x = element_blank(),
        panel.grid.minor.x = element_blank()
        ) +
      labs(x = "", y = "")

    p_hist / p_box
    ```

\vfill

\clearpage

11. **空气质量。** 每日空气质量由美国环境保护局报告的空气质量指数（AQI）来衡量。该指数报告污染水平以及可能令人担忧的相关健康影响。该指数针对《清洁空气法》规定的五种主要空气污染物计算，取值范围为 0 到 300，其中数值越高表示空气质量越差。2022 年，北卡罗来纳州达勒姆市报告了 356 天的 AQI 数据。下面的直方图显示了这些天 AQI 值的分布。[^_05-ex-explore-numerical-3]

    ```{r}
    #| fig-asp: 0.35
    ggplot(pm25_2022_durham, aes(x = daily_aqi_value)) +
      geom_histogram(aes(y = stat(count) / sum(count)),
                     binwidth = 5) +
      labs(x = "每日 AQI", y = "比例")
    ```

    a.  估计该样本的中位数 AQI 值。

    b.  你预计该样本的均值 AQI 值是高于还是低于中位数？解释你的推理。

    c.  估计该分布的 Q1、Q3 和 IQR。

    d.  该样本中是否有任何一天的 AQI 值会被认为是异常低或异常高？解释你的推理。

\vfill

12. **中位数与均值。** 估计直方图中显示的 400 个观测的中位数，并指出你预计均值是高于还是低于中位数。

    ```{r}
    #| fig-asp: 0.35
    set.seed(9823)
    df <- tibble(x = (100 * rbeta(400, 12, 3)))

    ggplot(df, aes(x = x)) +
      geom_histogram(binwidth = 5) +
      labs(x = "", y = "") +
      scale_y_continuous(breaks = seq(0, 80, 20))
    ```

\vfill

13. **直方图与箱线图。** 比较下面的两幅图。
    哪些分布特征在直方图中明显但在箱线图中不明显？
    哪些特征在箱线图中明显但在直方图中不明显？

    ```{r}
    #| fig-asp: 0.35
    set.seed(12345)

    df <- tibble(bimod = c(rnorm(300, mean = 5, sd = 1),
                           rnorm(300, mean = 12, sd = 1),
                           runif(25, min = 13, max = 28)))

    p_hist <- ggplot(df, aes(x = bimod)) +
      geom_histogram(binwidth = 2.5) +
      labs(x = NULL, y = NULL) +
      scale_x_continuous(breaks = seq(0, 30, 5), limits = c(0, 30))

    p_box <- ggplot(df, aes(y = bimod)) +
      geom_boxplot(outlier.alpha = 0.5) +
      theme(
        axis.text.x = element_blank(),
        panel.grid.major.x = element_blank(),
        panel.grid.minor.x = element_blank()
        ) +
      scale_y_continuous(breaks = seq(0, 30, 5), limits = c(0, 30)) +
      labs(x = NULL, y = NULL)

    p_hist + p_box
    ```

\vfill

\clearpage

14. **Facebook 好友数。** Facebook 数据表明，50%的Facebook用户拥有100个或更多好友，且用户的平均好友数为190。
    这些发现对 Facebook 用户好友数分布的形态有什么说明？
    [@Backstrom:2011]

\vfill

15. **分布与合适的统计量。** 对以下每种情况，判断你预计其分布是对称的、右偏的还是左偏的。
    同时说明均值和中位数哪个能最好地代表数据中的典型观测，以及观测的变异性用标准差还是四分位距（IQR）表示最好。
    解释你的推理。

    a.  每个家庭的宠物数量。

    b.  上班距离，即工作地点和家庭住址之间的英里数。

    c.  成年男性的身高。

    d.  死亡年龄。

    e.  简单考试的考试分数。

\vfill

16. **分布与合适的统计量。** 对以下每种情况，判断你预计其分布是对称的、右偏的还是左偏的。
    同时说明均值和中位数哪个能最好地代表数据中的典型观测，以及观测的变异性用标准差还是四分位距（IQR）表示最好。
    解释你的推理。

    a.  一个国家的房价，其中25%的房价低于\$350,000，50%的房价低于\$450,000，75%的房价低于\$1,000,000，且有相当数量的房价超过\$6,000,000。

    b.  一个国家的房价，其中25%的房价低于\$300,000，50%的房价低于\$600,000，75%的房价低于\$900,000，且很少有房价超过\$1,200,000。

    c.  大学生在给定一周内饮用的酒精饮料数量。
        假设这些学生中大多数由于未满 21 岁不饮酒，只有少数学生过量饮酒。

    d.  一家财富 500 强公司员工的年薪，其中只有少数高层管理人员的薪资远高于其他所有员工。

    e.  人类的妊娠时间，其中25%的婴儿在妊娠38周时出生，50%在39周时出生，75%在40周时出生，最长妊娠时间为46周。

\vfill

17. **电视观看者。** 某统计学班上的大学生被问及他们每周观看多少小时电视，包括在线流媒体服务。
    该样本的平均数为 8.28 小时，标准差为 7.18 小时。
    学生每周观看电视时长的分布是对称的吗？
    如果不是，你预计这个分布会是什么形状？
    解释你的推理。

\vfill

18. **考试分数。** 一次历史考试的平均数（满分 100 分）为 85 分，标准差为 15 分。
    这次考试分数的分布是对称的吗？
    如果不是，你预计这个分布会是什么形状？
    解释你的推理。

\vfill

19. **中距。** *中距* 定义为分布中最大值与最小值的平均数。
    这个统计量对离群值和极端偏态稳健吗？
    解释你的推理。

\vfill

\clearpage

20. **奥斯卡奖得主。** 首届奥斯卡最佳男主角和最佳女主角奖项于 1929 年颁发。
    下面的直方图显示了 1929 年至 2019 年间所有最佳男主角和最佳女主角获奖者的年龄分布。
    同时还提供了这些分布的汇总统计量。
    比较最佳男主角和女主角获奖者的年龄分布。[^_05-ex-explore-numerical-4]

    :::: {layout="[-0.05, 0.6, 0.35]"}

    :::{#plot}
    ```{r}
    #| fig-width: 5
    ggplot(oscars, aes(x = age)) +
      geom_histogram(binwidth = 10) +
      facet_wrap(~fct_rev(award), ncol = 1) +
      labs(x = "年龄（岁）", y = NULL)
    ```
    :::
    :::{#table}
    ```{r}
    oscars |>
      group_by(award) |>
      summarise(
        Mean = mean(age),
        SD = sd(age),
        n = n()
      ) |>
      kbl(linesep = "", booktabs = TRUE, col.names = c("", "均值", "标准差", "n"), digits = 1) |>
      kable_styling(bootstrap_options = c("striped", "condensed"),
                    latex_options = "HOLD_position",
                    full_width = FALSE)
    ```
    :::
    ::::

\vfill

21.  **统计分数。** 将二十名统计学入门学生的期末考试分数按升序排列如下：57, 66, 69, 71, 72, 73, 74, 77, 78, 78, 79, 79, 81, 81, 82, 83, 83, 88, 89, 94。
    假设期末考试分数高于第 75 百分位数的学生能在这门课中得到 A。
    那么，这门课中会有多少学生得到 A？

\vfill

22.  **咖啡店的收入。** 下面的第一幅直方图展示了某大学咖啡店 40 位顾客的年收入分布。
    假设有两位新顾客走进咖啡店：一位年收入为 \$225,000，另一位为 \$250,000。
    第二幅直方图展示了新的收入分布。
    同时提供了四舍五入至最接近整数的汇总统计量。

    ```{r}
    set.seed(956)

    sal_symmetric <- c(rnorm(40, mean = 65000, sd = 2000))
    sal_skewed    <- c(sal_symmetric, 225000, 250000)

    df <- tibble(sal_symmetric = c(sal_symmetric, NA, NA), sal_skewed = sal_skewed) |>
      pivot_longer(cols = everything(), names_to = "type", values_to = "x") |>
      mutate(type_label = if_else(type == "sal_symmetric", "之前", "之后"))

    # plots

    p_sym <- df |>
      filter(type == "sal_symmetric") |>
      ggplot(aes(x = x)) +
      geom_histogram(binwidth = 1000) +
      scale_x_continuous(limits = c(60000, 70000), breaks = seq(60000, 70000, 2000),     labels = label_dollar(scale = 0.001, suffix = "K", accuracy = 1)) +
      scale_y_continuous(limits = c(0, 10), breaks = seq(0, 10, 2)) +
      labs(x = NULL, y = NULL, title = "之前")

    p_skew <- df |>
      filter(type == "sal_skewed") |>
      ggplot(aes(x = x)) +
      geom_histogram(binwidth = 1000) +
      scale_x_continuous(limits = c(60000, 260000), breaks = seq(60000, 260000, 50000), labels = label_dollar(scale = 0.001, suffix = "K", accuracy = 1)) +
      scale_y_continuous(limits = c(0, 10), breaks = seq(0, 10, 2)) +
      labs(x = NULL, y = NULL, title = "之后")

    p_sym / p_skew

    # table

    df |>
      mutate(type_label = fct_relevel(type_label, "之前", "之后")) |>
      group_by(type_label) |>
      summarise(
        n      = sum(!is.na(x)),
        Min    = min(x, na.rm = TRUE),
        Q1     = quantile(x, probs = 0.025, na.rm = TRUE),
        Median = median(x, na.rm = TRUE),
        Mean   = mean(x, na.rm = TRUE),
        Max    = max(x, na.rm = TRUE),
        SD     = sd(x, na.rm = TRUE)
      ) |>
      mutate(across(Min:SD, label_dollar(accuracy = 1))) |>
      kbl(linesep = "", booktabs = TRUE, align = "rrrrrrr",
          col.names = c("", "n", "最小值", "Q1", "中位数", "均值", "最大值", "标准差"), digits = 0) |>
      kable_styling(bootstrap_options = c("striped", "condensed"),
                    latex_options = "HOLD_position",
                    full_width = FALSE)
    ```

    a.  均值和中位数，哪一个能更好地代表我们可能认为的这 42 位咖啡店顾客的“典型”收入？
        这说明了这两个度量指标的什么稳健性？

    b.  标准差和四分位距（IQR），哪一个能更好地代表这 42 位咖啡店顾客收入的变异性？
        这说明了这两个度量指标的什么稳健性？

\vfill

\clearpage

23.  **一个新统计量。** 统计量 $\frac{\bar{x}}{median}$ 可以用来衡量偏度。
    假设我们有一个分布，其中的所有观测值都大于 0，即 $x_i > 0$。
    在以下条件下，分布的预期形状是什么？
    请解释你的推理。

    a.  $\frac{\bar{x}}{median} = 1$

    b.  $\frac{\bar{x}}{median} < 1$

    c.  $\frac{\bar{x}}{median} > 1$

\vfill

24.  **通勤时间。** 美国人口普查收集美国人上班通勤所需时间的数据，以及许多其他变量。
    下面的直方图展示了 2017 年美国 3,142 个县的平均通勤时间分布。
    下面还显示了同一数据的空间强度图。[^_05-ex-explore-numerical-5]

    ```{r}
    #| out-width: 95%
    #| fig-asp: 0.3
    #| fig-width: 10
    dfips <- maps::county.fips |>
      as_tibble() |>
      extract(polyname, c("region", "subregion"), "^([^,]+),([^,]+)$")

    map_county <- map_data("county") |>
      as_tibble() |>
      left_join(dfips) |>
      mutate(fips = case_when(
        subregion == "okaloosa"  & region == "florida"        ~ 12091L,
        subregion == "st martin" & region == "louisiana"      ~ 22099L,
        subregion == "currituck" & region == "north carolina" ~ 37053L,
        # Oglala Lakota Count, see     https://en.wikipedia.org/wiki/Oglala_Lakota_County,_South_Dakota
        subregion == "shannon"   & region == "south dakota"   ~ 46113L,
        subregion == "galveston" & region == "texas"          ~ 48167L,
        subregion == "accomack"  & region == "virginia"       ~ 51001L,
        subregion == "pierce"    & region == "washington"     ~ 53053L,
        subregion == "san juan"  & region == "washington"     ~ 53055L,
        TRUE ~ fips
      ))

    county_for_map <- county_complete |>
      select(fips, name, state, mean_work_travel_2017)

    map_county <- map_county |>
      left_join(county_for_map, by = "fips")

    p_hist <- ggplot(county_complete, aes(x = mean_work_travel_2017)) +
      geom_histogram(binwidth = 5) +
      labs(x = "平均通勤时间\n（分钟）", y = NULL)

    p_map <- ggplot(map_county, aes(x = long, y = lat, group = group)) +
      geom_polygon(aes(fill = mean_work_travel_2017)) +
      scale_fill_viridis_c(option = "D") +
      labs(x = NULL, y = NULL, fill = "平均通勤\n时间（分钟）") +
      theme(
        axis.text = element_blank(),
        axis.ticks = element_blank(),
        panel.grid.major = element_blank(),
        panel.grid.minor = element_blank()
      ) +
      coord_quickmap()

    p_hist + p_map + guide_area() + plot_layout(ncol = 3, widths = c(2, 3, 0.5), guides = "collect", axis_titles = "collect")
    ```

    a.  描述该数值分布，并讨论对这些数据是否适合进行对数变换。

    b.  利用地图描述通勤时间的空间分布。

\vfill

25. **西班牙裔人口。** 美国人口普查收集有关美国人种族和族裔的数据，以及许多其他变量的数据。
    下面的直方图展示了2010年美国3,142个县中西班牙裔人口百分比的分布。
    同时展示了这些值取对数后的直方图。[^_05-ex-explore-numerical-6]

    ```{r}
    #| out-width: 95%
    #| fig-asp: 0.3
    #| fig-width: 10
    dfips <- maps::county.fips |>
      as_tibble() |>
      extract(polyname, c("region", "subregion"), "^([^,]+),([^,]+)$")

    map_county <- map_data("county") |>
      as_tibble() |>
      left_join(dfips) |>
      mutate(fips = case_when(
        subregion == "okaloosa"  & region == "florida"        ~ 12091L,
        subregion == "st martin" & region == "louisiana"      ~ 22099L,
        subregion == "currituck" & region == "north carolina" ~ 37053L,
        # Oglala Lakota Count, see     https://en.wikipedia.org/wiki/Oglala_Lakota_County,_South_Dakota
        subregion == "shannon"   & region == "south dakota"   ~ 46113L,
        subregion == "galveston" & region == "texas"          ~ 48167L,
        subregion == "accomack"  & region == "virginia"       ~ 51001L,
        subregion == "pierce"    & region == "washington"     ~ 53053L,
        subregion == "san juan"  & region == "washington"     ~ 53055L,
        TRUE ~ fips
      ))

    county_for_map <- county_complete |>
      select(fips, name, state, hispanic_2017)

    map_county <- map_county |>
      left_join(county_for_map, by = "fips")

    p_hist <- ggplot(county_complete, aes(x = hispanic_2017)) +
      geom_histogram(binwidth = 5) +
      scale_x_continuous(labels = label_percent(scale = 1)) +
      labs(x = "% Hispanic", y = NULL)

    p_hist_log <- ggplot(county_complete, aes(x = log(hispanic_2017))) +
      geom_histogram(binwidth = 0.2) +
      labs(x = "log(% Hispanic)", y = NULL)

    p_map <- ggplot(map_county, aes(x = long, y = lat, group = group)) +
      geom_polygon(aes(fill = hispanic_2017)) +
      scale_fill_viridis_c(option = "E", labels = label_percent(scale = 1)) +
      labs(x = NULL, y = NULL, fill = "% Hispanic") +
      theme(
        axis.text = element_blank(),
        axis.ticks = element_blank(),
        panel.grid.major = element_blank(),
        panel.grid.minor = element_blank()
        ) +
      coord_quickmap()

    p_hist + p_hist_log + p_map + plot_layout(widths = c(1, 1, 2))
    ```

    a.  描述该数值分布，并说明在分析或建模这些数据时，为什么我们可能希望使用对数变换后的值。

b.  美国各县西班牙裔人口分布的哪些特征在地图中很明显，而在直方图中不明显？
        哪些特征在直方图中很明显，而在地图中不明显？

    c.  其中一种可视化方式是否比另一种更合适或更有帮助？
        请解释你的理由。

\clearpage

26. **纽约市马拉松冠军。** 下面的直方图和箱线图显示了 1970 年至 2023 年间纽约市马拉松男性与女性（合并）冠军完赛时间的分布。[^_05-ex-explore-numerical-7]

    ```{r}
    #| fig-asp: 0.5
    p_hist <- ggplot(nyc_marathon, aes(x = time_hrs)) +
      geom_histogram(binwidth = 0.05) +
      labs(x = "马拉松时间", y = "频数") +
      scale_x_continuous(limits = c(2, 3.2), breaks = seq(2, 3.2, 0.4))

    p_box <- ggplot(nyc_marathon, aes(x = time_hrs)) +
      geom_boxplot(outlier.alpha = 0.8) +
      labs(x = "马拉松时间") +
      theme(
        panel.grid.major.y = element_blank(),
        panel.grid.minor.y = element_blank(),
        axis.text.y = element_blank()
      ) +
      scale_x_continuous(limits = c(2, 3.2), breaks = seq(2, 3.2, 0.4))

    p_hist / p_box
    ```

    a.  该分布的哪些特征在直方图中很明显，而在箱线图中不明显？
        哪些特征在箱线图中很明显，而在直方图中不明显？

    b.  出现双峰分布的原因可能是什么？
        请解释。

    c.  根据下图所示的箱线图，比较男性和女性的马拉松完赛时间分布。

    ```{r}
    #| out-width: 95%
    #| fig-asp: 0.4
    ggplot(nyc_marathon, aes(x = time_hrs, y = division, color = division)) +
      geom_boxplot(outlier.alpha = 0.8, show.legend = FALSE) +
      labs(x = "马拉松时间", y = NULL) +
      scale_x_continuous(limits = c(2, 3.2), breaks = seq(2, 3.2, 0.4)) +
      scale_color_manual(values = c(IMSCOL["blue","full"], IMSCOL["red","full"]))
    ```

    d.  下图所示的时间序列图是查看这些数据的另一种方式。请描述在此图中可见，而在其他图中不可见的内容。

    ```{r}
    #| out-width: 95%
    #| fig-asp: 0.4
    ggplot(nyc_marathon, aes(x = year, y = time_hrs,
                             color = division, shape = division)) +
      geom_point(size = 2) +
      labs(y = "马拉松时间", x = "年份") +
      scale_color_manual(values = c(IMSCOL["blue","full"], IMSCOL["red","full"]))
    ```

[^_05-ex-explore-numerical-1]: 本练习使用的 [`mammals`](http://openintrostat.github.io/openintro/reference/mammals.html) 数据可在 [**openintro**](http://openintrostat.github.io/openintro) R 包中找到。

[^_05-ex-explore-numerical-2]: 本练习使用的 [`cia_factbook`](http://openintrostat.github.io/openintro/reference/cia_factbook.html) 数据可在 [**openintro**](http://openintrostat.github.io/openintro) R 包中找到。

[^_05-ex-explore-numerical-3]: 本练习使用的 [`pm25_2022_durham`](http://openintrostat.github.io/openintro/reference/pm25_2022_durham.html) 数据可在 [**openintro**](http://openintrostat.github.io/openintro) R 包中找到。

[^_05-ex-explore-numerical-4]: 本练习使用的 [`oscars`](http://openintrostat.github.io/openintro/reference/oscars.html) 数据可在 [**openintro**](http://openintrostat.github.io/openintro) R 包中找到。

[^_05-ex-explore-numerical-5]: 本练习使用的 [`county_complete`](http://openintrostat.github.io/openintro/reference/county_complete.html) 数据可在 [**usdata**](http://openintrostat.github.io/usdata) R 包中找到。

[^_05-ex-explore-numerical-6]: 本练习使用的 [`county_complete`](http://openintrostat.github.io/openintro/reference/county_complete.html) 数据可在 [**usdata**](http://openintrostat.github.io/usdata) R 包中找到。

[^_05-ex-explore-numerical-7]: 本练习使用的 [`nyc_marathon`](http://openintrostat.github.io/openintro/reference/nyc_marathon.html) 数据可在 [**openintro**](http://openintrostat.github.io/openintro) R 包中找到。